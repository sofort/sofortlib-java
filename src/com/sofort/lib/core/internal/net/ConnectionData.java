package com.sofort.lib.core.internal.net;

/**
 * An abstract definition of a connection data. No public methods defined,
 * because different kinds of connection need the different connection data.
 */
public interface ConnectionData {
	/* NoOp */
}
